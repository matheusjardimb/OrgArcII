## Código ##

Linhas iniciadas com # serão comentários. 
Label's devem ocupar toda a linha.
Comandos do tipo 'i' (e.g. addi, ori...) devem ter a constante como último parâmetro


Lista de comandos suportados:

- ADD, AND, OR, SLT
- ADDI, ANDI, ORI, SLTI
- LW, SW
- BEQ
- J
- NOP (código de máquina 0x00000000, interpretado pelo processador como o sll $0, $0, 0)


Alteracao
